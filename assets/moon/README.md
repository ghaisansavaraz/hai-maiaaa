# Moon Texture Fallback

This folder can hold a local fallback texture for the moon icon.

- Preferred file name: `moonmap.jpg` (or `.webp`)
- Recommended size: 512–1024 px square
- Source suggestion (public domain): NASA SVS moon textures

If the remote NASA URL is unavailable, you can place your texture here and swap the SVG `<image>` `href` to:

```
assets/moon/moonmap.jpg
```


